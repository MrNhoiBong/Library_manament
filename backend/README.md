# 🚀 Backend API Setup Guide

Đây là hướng dẫn để thiết lập và chạy dự án backend sử dụng FastAPI và Uvicorn.


## 🧰 Yêu cầu hệ thống

- Python 3.8 trở lên
- pip (Python package installer)

## 📦 Cài đặt thư viện

Trước tiên, hãy đảm bảo bạn đang ở trong thư mục `backend`. Sau đó chạy:

```bash
pip install -r requirements.txt
```
Sau đó chạy backend:
 ```bash
python backend.py
```

Truy cập vào http://localhost:8000/docs để đảm bảo server chạy.

Lưu ý: khởi chạy database Mysql trước khi chạy server
